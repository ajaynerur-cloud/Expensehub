(()=>{'use strict';const R=document.querySelector('#root'),D=document.querySelector('#dlg'),F=document.querySelector('#entry'),$=s=>document.querySelector(s);let S={token:localStorage.eh12||'',user:null,projects:[],pid:localStorage.eh12p||'',project:null,folders:[],records:[],members:[],view:'dashboard',batch:[],mode:'mass',invite:new URLSearchParams(location.search).get('invite')||''};const E=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),M=n=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR'}).format(+n||0),toast=x=>{let t=$('#toast');t.textContent=x;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1800)};async function api(u,o={}){let r=await fetch(u,{...o,headers:{'Content-Type':'application/json',...(S.token?{Authorization:`Bearer ${S.token}`}:{})}}),j=await r.json().catch(()=>({}));if(!r.ok){let x=Error(j.error||r.status);x.status=r.status;throw x}return j}const auth=h=>R.innerHTML=`<section class="auth"><div class="box">${h}</div></section>`;function login(msg='',email=''){auth(`<h1>ExpenseHub V12</h1>${msg?`<p>${E(msg)}</p>`:''}<form id="login"><label>Email<input name="email" type="email" value="${E(email)}" required></label><label>Password<input name="password" type="password" required></label><button class="primary">Sign in</button></form><p><button id="signup" class="secondary">Create account</button></p>`);$('#signup').onclick=signup;$('#login').onsubmit=async x=>{x.preventDefault();try{let d=await api('/api/auth/login',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(x.target)))});S.token=d.token;localStorage.eh12=S.token;boot()}catch(y){alert(y.message)}}}function signup(){auth(`<h1>Create account</h1><form id="sf"><label>Name<input name="name" required></label><label>Email<input name="email" type="email" required></label><label>Password<input name="password" type="password" minlength="8" required></label><label>Confirm<input name="confirm" type="password" required></label><button class="primary">Create</button></form>`);$('#sf').onsubmit=async x=>{x.preventDefault();let b=Object.fromEntries(new FormData(x.target));if(b.password!==b.confirm)return alert('Passwords do not match');delete b.confirm;let d=await api('/api/auth/signup',{method:'POST',body:JSON.stringify(b)});login(d.message,d.email)}}async function boot(){try{let d=await api('/api/me');S.user=d.user;S.projects=d.projects;if(S.invite)return invite();if(!S.projects.length)return home();if(!S.projects.some(x=>x.id===S.pid))S.pid=S.projects[0].id;await load(S.pid);shell();render()}catch{localStorage.removeItem('eh12');S.token='';login('Please sign in.')}}async function invite(){let i=await api(`/api/invites/${S.invite}`);auth(`<h1>Project invitation</h1><h2>${E(i.project.name)}</h2><button class="primary" id="join">Join project</button> <button class="secondary" id="own">Create my project</button>`);$('#join').onclick=async()=>{let d=await api(`/api/invites/${S.invite}/join`,{method:'POST'});S.invite='';history.replaceState({},'','/');S.pid=d.projectId;boot()};$('#own').onclick=()=>createProject(true)}function home(){R.innerHTML=`<header class="header"><div class="brand"><img src="/assets/icon.svg"><b>ExpenseHub V12</b></div><span></span><button id="logout">Log out</button></header><section class="content"><div class="hero"><h1>My projects</h1><button id="new">+ New</button></div><div class="toolbar"><button class="primary" id="excelAll">Export all to Excel</button></div><div class="projects">${S.projects.map(p=>`<div class="projectCard"><h3>${E(p.name)}</h3><p>${p.role}</p><button class="open primary" data-id="${p.id}">Open</button></div>`).join('')}</div></section>`;$('#logout').onclick=logout;$('#new').onclick=()=>createProject(false);$('#excelAll').onclick=exportAll;document.querySelectorAll('.open').forEach(b=>b.onclick=async()=>{await load(b.dataset.id);shell();render()})}function createProject(back){auth(`<h1>Create project</h1><form id="np"><label>Name<input name="name" required></label><label>Description<input name="description"></label><button class="primary">Create</button></form>`);$('#np').onsubmit=async x=>{x.preventDefault();let p=await api('/api/projects',{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(x.target)))});S.pid=p.id;back?invite():boot()}}async function load(id){let d=await api(`/api/projects/${id}/data`);S.pid=id;localStorage.eh12p=id;S.project=d.project;S.folders=d.folders;S.records=d.records;S.members=d.members}function shell(){R.innerHTML=`<header class="header"><button id="menu" class="menu">☰</button><div class="brand"><img src="/assets/icon.svg"><b>ExpenseHub V12</b></div><select id="ps" class="ps"><option value="home">All projects</option>${S.projects.map(p=>`<option value="${p.id}" ${p.id===S.pid?'selected':''}>${E(p.name)} (${p.role})</option>`).join('')}</select><button id="logout">↪</button></header><div id="scrim" class="scrim"></div><div class="shell"><aside id="side"><nav><button data-v="dashboard">Dashboard</button><button data-v="analytics">Folder analytics</button><button data-a="single">Single add</button><button data-a="mass">Mass add</button><button data-v="personal">Personal</button><button data-v="vendor">Vendor</button><button data-v="received">Received</button><button data-v="folders">Folders</button><button id="load" data-a="load">Load CSV / Excel</button><button data-v="members">Members & owners</button><button data-v="settings">Export & settings</button><button data-v="account">Exit ExpenseHub</button></nav><div id="status">${E(S.project.name)} · ${S.project.role}</div></aside><section class="content" id="app"></section></div><nav class="bottom"><button data-v="dashboard">Home</button><button data-a="single">Add</button><button data-v="analytics">Analytics</button><button id="more">More</button></nav>`;$('#logout').onclick=logout;$('#ps').onchange=async x=>{if(x.target.value==='home')return home();await load(x.target.value);shell();render()};let close=()=>{$('#side').classList.remove('open');$('#scrim').classList.remove('show')};$('#menu').onclick=$('#more').onclick=()=>{$('#side').classList.toggle('open');$('#scrim').classList.toggle('show')};$('#scrim').onclick=close;document.querySelectorAll('[data-v]').forEach(b=>b.onclick=()=>{S.view=b.dataset.v;close();render()});document.querySelectorAll('[data-a]').forEach(b=>b.onclick=()=>{close();if(b.dataset.a==='single')return open();S.mode=b.dataset.a;S.batch=[];S.view='batch';render()});if(window.Capacitor?.isNativePlatform?.())$('#load')?.remove()}const logout=()=>{localStorage.removeItem('eh12');location.href='/'};async function save(){let d=await api(`/api/projects/${S.pid}/data`,{method:'PUT',body:JSON.stringify({folders:S.folders,records:S.records})});S.folders=d.folders;S.records=d.records;toast('Saved')}const rows=t=>S.records.filter(x=>!t||x.type===t).sort((a,b)=>String(b.date).localeCompare(String(a.date))),folder=id=>S.folders.find(x=>x.id===id)?.name||'General';function table(a){return `<div class="table desktopTable"><table><tr><th>Date</th><th>Type</th><th>Party</th><th>Folder</th><th>Mode</th><th>Amount</th><th></th></tr>${a.map(x=>`<tr><td>${x.date}</td><td>${x.type}</td><td>${E(x.party)}</td><td>${E(folder(x.folderId))}</td><td>${E(x.mode)}</td><td><b>${M(x.amount)}</b></td><td><button class="edit" data-id="${x.id}">✎</button><button class="del" data-id="${x.id}">🗑</button></td></tr>`).join('')||'<tr><td colspan="7">No records</td></tr>'}</table></div><div class="mobileCards">${a.map(x=>`<div class="txn"><div class="txnTop"><b>${E(x.party)}</b><strong>${M(x.amount)}</strong></div><div class="txnMeta"><span>${x.date}</span><span>${x.type}</span><span>${E(folder(x.folderId))}</span><span>${E(x.mode)}</span></div><div class="txnActions"><button class="edit" data-id="${x.id}">Edit</button><button class="del danger" data-id="${x.id}">Delete</button></div></div>`).join('')||'<div class="panel">No records</div>'}</div>`}function summary(){let x={};S.records.forEach(r=>{let n=folder(r.folderId);if(!x[n])x[n]={name:n,total:0,count:0};if(r.type!=='received')x[n].total+=+r.amount;x[n].count++});return Object.values(x).sort((a,b)=>b.total-a.total)}function render(){let A=$('#app');if(S.view==='dashboard'){let t=k=>S.records.filter(x=>x.type===k).reduce((a,x)=>a+ +x.amount,0),s=summary();A.innerHTML=`<div class="hero"><h1>${E(S.project.name)}</h1><button id="add">+ Add</button></div><div class="stats"><div class="card">Personal<strong>${M(t('personal'))}</strong></div><div class="card">Vendor<strong>${M(t('vendor'))}</strong></div><div class="card">Received<strong>${M(t('received'))}</strong></div></div><div class="panel"><h2>Folder summary</h2>${s.map(x=>`<div class="rank"><span>${E(x.name)} (${x.count})</span><div class="bar"><span style="width:${x.total/(s[0]?.total||1)*100}%"></span></div><b>${M(x.total)}</b></div>`).join('')||'No data'}</div>${table(rows().slice(0,10))}`;$('#add').onclick=()=>open()}else if(S.view==='analytics'){let s=summary();A.innerHTML=`<div class="hero"><h1>Folder analytics</h1></div><div class="panel">${s.map((x,i)=>`<div class="rank"><span>${i+1}. ${E(x.name)}</span><div class="bar"><span style="width:${x.total/(s[0]?.total||1)*100}%"></span></div><b>${M(x.total)}</b></div>`).join('')||'No data'}</div>`}else if(['personal','vendor','received'].includes(S.view)){A.innerHTML=`<div class="hero"><h1>${S.view}</h1><button id="add">+ Add</button></div>${table(rows(S.view))}`;$('#add').onclick=()=>open(null,S.view)}else if(S.view==='folders')folders(A);else if(S.view==='batch')batch(A);else if(S.view==='members')members(A);else if(S.view==='settings')settings(A);else account(A);A.onclick=actions}function open(x=null,t='personal'){F.reset();F.id.value=x?.id||'';F.type.value=x?.type||t;F.folderId.innerHTML='<option value="">General</option>'+S.folders.map(f=>`<option value="${f.id}">${E(f.name)}</option>`).join('');for(let k of['folderId','party','amount','date','mode','notes'])if(x?.[k]!=null)F[k].value=x[k];if(!x)F.date.value=new Date().toISOString().slice(0,10);D.showModal()}F.onsubmit=async x=>{x.preventDefault();let b=Object.fromEntries(new FormData(F));b.amount=+b.amount;if(b.id){let i=S.records.findIndex(r=>r.id===b.id);S.records[i]={...S.records[i],...b}}else S.records.push({...b,id:crypto.randomUUID(),createdBy:S.user.id});await save();D.close();render()};$('#cancel').onclick=()=>D.close();function folders(A){A.innerHTML=`<div class="hero"><h1>Folders</h1><button id="nf">+ New</button></div><div class="folders">${S.folders.map(f=>`<div class="folder"><h3>${E(f.name)}</h3><button class="fdel danger" data-id="${f.id}">Delete</button></div>`).join('')}</div>`;$('#nf').onclick=async()=>{let n=prompt('Folder name');if(n){S.folders.push({id:crypto.randomUUID(),name:n,createdBy:S.user.id});await save();render()}}}const blank=()=>({selected:true,date:new Date().toISOString().slice(0,10),party:'',amount:0,mode:'Bank Transfer',notes:'',type:'personal',folderId:''});function batch(A){if(!S.batch.length)S.batch=Array.from({length:5},blank);let fo='<option value="">General</option>'+S.folders.map(f=>`<option value="${f.id}">${E(f.name)}</option>`).join('');A.innerHTML=`<div class="hero"><h1>${S.mode==='load'?'Load CSV / Excel':'Mass add'}</h1><button id="moreRow">+ Row</button></div>${S.mode==='load'?'<div class="drop"><input id="file" type="file" accept=".csv,.xlsx,.xls"></div>':''}<div class="toolbar"><button class="primary" id="saveBatch">Save selected</button></div><div class="table"><table class="batch"><tr><th>Use</th><th>Date</th><th>Party</th><th>Amount</th><th>Mode</th><th>Notes</th><th>Type</th><th>Folder</th></tr>${S.batch.map((x,i)=>`<tr><td><input class="bi" data-i="${i}" data-k="selected" type="checkbox" ${x.selected?'checked':''}></td><td><input class="bi" data-i="${i}" data-k="date" type="date" value="${x.date}"></td><td><input class="bi" data-i="${i}" data-k="party" value="${E(x.party)}"></td><td><input class="bi" data-i="${i}" data-k="amount" type="number" value="${x.amount}"></td><td><input class="bi" data-i="${i}" data-k="mode" value="${E(x.mode)}"></td><td><input class="bi" data-i="${i}" data-k="notes" value="${E(x.notes)}"></td><td><select class="bi" data-i="${i}" data-k="type"><option>personal</option><option>vendor</option><option>received</option></select></td><td><select class="bi" data-i="${i}" data-k="folderId">${fo}</select></td></tr>`).join('')}</table></div>`;document.querySelectorAll('.bi').forEach(n=>n.onchange=x=>{let k=x.target.dataset.k;S.batch[+x.target.dataset.i][k]=k==='selected'?x.target.checked:k==='amount'?+x.target.value:x.target.value});$('#moreRow').onclick=()=>{S.batch.push(blank());batch(A)};$('#saveBatch').onclick=async()=>{let v=S.batch.filter(x=>x.selected&&x.date&&x.party&&x.amount>0);v.forEach(x=>S.records.push({...x,id:crypto.randomUUID(),createdBy:S.user.id}));await save();S.batch=[];S.view='dashboard';render()};if($('#file'))$('#file').onchange=importFile}async function importFile(ev){let wb=XLSX.read(await ev.target.files[0].arrayBuffer(),{type:'array',cellDates:true}),r=XLSX.utils.sheet_to_json(wb.Sheets[wb.SheetNames[0]],{defval:''});S.batch=r.map(x=>{let g=(...n)=>{for(let a of n){let k=Object.keys(x).find(b=>b.trim().toLowerCase()===a.toLowerCase());if(k)return x[k]}return''},d=g('Date');if(d instanceof Date)d=d.toISOString().slice(0,10);return{selected:true,date:String(d).slice(0,10),party:String(g('Vendor','Person','Name','Party')),amount:+String(g('Amount')).replace(/[,₹$]/g,''),mode:String(g('Mode')||'Other'),notes:String(g('Purpose','Notes')),type:'vendor',folderId:''}});batch($('#app'))}async function members(A){A.innerHTML=`<div class="hero"><h1>Members & owners</h1>${S.project.role==='owner'?'<button id="invite">QR invite</button>':''}</div><div class="panel">${S.members.map(m=>`<div class="member"><div><b>${E(m.user.name)}</b><small>${E(m.user.email)} · ${m.role} · ${m.active===false?'disabled':'active'}</small></div>${S.project.role==='owner'&&m.userId!==S.user.id?`<div class="memberActions"><button class="role" data-id="${m.userId}" data-role="${m.role==='owner'?'member':'owner'}">${m.role==='owner'?'Make member':'Promote owner'}</button><button class="toggle" data-id="${m.userId}">${m.active===false?'Enable':'Disable'}</button><button class="remove danger" data-id="${m.userId}">Remove</button></div>`:''}</div>`).join('')}</div>`;document.querySelectorAll('.role').forEach(b=>b.onclick=async()=>{try{await api(`/api/projects/${S.pid}/members/${b.dataset.id}/role`,{method:'POST',body:JSON.stringify({role:b.dataset.role})});await load(S.pid);render()}catch(x){alert(x.message)}});document.querySelectorAll('.toggle').forEach(b=>b.onclick=async()=>{try{await api(`/api/projects/${S.pid}/members/${b.dataset.id}/toggle`,{method:'POST'});await load(S.pid);render()}catch(x){alert(x.message)}});document.querySelectorAll('.remove').forEach(b=>b.onclick=async()=>{let p=prompt('Enter owner password to remove this member');if(p===null)return;try{await api(`/api/projects/${S.pid}/members/${b.dataset.id}`,{method:'DELETE',body:JSON.stringify({password:p})});await load(S.pid);render()}catch(x){alert(x.message)}});if($('#invite'))$('#invite').onclick=async()=>{let x=await api(`/api/projects/${S.pid}/invite`,{method:'POST'});A.innerHTML=`<div class="panel"><img class="qr" src="${x.qr}"><input value="${x.url}" style="width:100%;padding:10px"></div>`}}function settings(A){A.innerHTML=`<div class="hero"><h1>Export & settings</h1></div><div class="panel"><button class="primary" id="excelCurrent">Export current</button> <button id="excelAll">Export all</button></div>${S.project.role==='owner'?`<div class="panel dangerZone"><h2>Delete entire project</h2><p>Owner can permanently delete the project without promoting another owner. This deletes every member, invitation, folder and transaction in this project.</p><input id="dp" type="password" placeholder="Owner password"><input id="dt" placeholder="Type DELETE"><button class="danger" id="deleteProject">Delete entire project</button></div>`:''}<div class="panel dangerZone"><h2>Leave project</h2><input id="lp" type="password" placeholder="Password"><input id="lt" placeholder="Type LEAVE"><button class="danger" id="leave">Leave project</button></div>`;$('#excelCurrent').onclick=()=>exportBook({projects:[S.project],folders:S.folders,records:S.records,members:S.members});$('#excelAll').onclick=exportAll;if($('#deleteProject'))$('#deleteProject').onclick=deleteProject;$('#leave').onclick=leave}function account(A){A.innerHTML=`<div class="hero"><h1>Exit ExpenseHub</h1></div><div class="panel dangerZone"><input id="ep" type="password" placeholder="Password"><input id="et" placeholder="Type EXIT"><button class="danger" id="exit">Delete account and exit</button></div>`;$('#exit').onclick=exitAll}async function exportAll(){exportBook(await api('/api/export'))}function exportBook(d){let wb=XLSX.utils.book_new(),p=d.projects||[S.project],f=d.folders||[],r=d.records||[],mm=d.members||[];XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(p),'Projects');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(r),'Transactions');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(f),'Folders');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(mm),'Members');XLSX.writeFile(wb,`ExpenseHub-V12-${new Date().toISOString().slice(0,10)}.xlsx`)}async function deleteProject(){if(!confirm('Delete the complete project and all data? This cannot be undone.'))return;try{let d=await api(`/api/projects/${S.pid}`,{method:'DELETE',body:JSON.stringify({password:$('#dp').value,confirmation:$('#dt').value})});alert(d.message);S.pid='';localStorage.removeItem('eh12p');boot()}catch(x){alert(x.message)}}async function leave(){try{let d=await api(`/api/projects/${S.pid}/leave`,{method:'DELETE',body:JSON.stringify({password:$('#lp').value,confirmation:$('#lt').value})});alert(d.message);boot()}catch(x){alert(x.message)}}async function exitAll(){try{let d=await api('/api/account/exit',{method:'DELETE',body:JSON.stringify({password:$('#ep').value,confirmation:$('#et').value})});alert(d.message);localStorage.clear();location.href='/'}catch(x){alert(x.message)}}async function actions(x){let id=x.target.dataset.id;if(x.target.classList.contains('edit'))open(S.records.find(y=>y.id===id));if(x.target.classList.contains('del')){S.records=S.records.filter(y=>y.id!==id);await save();render()}if(x.target.classList.contains('fdel')){S.folders=S.folders.filter(y=>y.id!==id);S.records.forEach(y=>{if(y.folderId===id)y.folderId=''});await save();render()}}S.token?boot():login()})();
